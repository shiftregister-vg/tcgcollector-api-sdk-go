name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.24'
          cache: true

      - name: Get dependencies
        run: go mod download

      - name: Run tests
        run: go test ./... -v -race

      - name: Create Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          
          # Get the previous tag
          PREV_TAG=$(git describe --tags --abbrev=0 $TAG^ 2>/dev/null || echo "")
          
          # Generate release notes
          if [ -z "$PREV_TAG" ]; then
            CHANGES=$(git log --pretty=format:"- %s (%h)" $TAG)
            CONTRIBUTORS=$(git log --pretty=format:"- %an" $TAG | sort -u)
            NOTES=$(cat <<'EOF'
# Release $TAG

## Changes

$CHANGES

## Contributors

$CONTRIBUTORS
EOF
)
          else
            CHANGES=$(git log --pretty=format:"- %s (%h)" $PREV_TAG..$TAG)
            CONTRIBUTORS=$(git log --pretty=format:"- %an" $PREV_TAG..$TAG | sort -u)
            NOTES=$(cat <<'EOF'
# Release $TAG

## Changes since $PREV_TAG

$CHANGES

## Contributors

$CONTRIBUTORS
EOF
)
          fi
          
          # Create the release
          gh release create $TAG \
            --title "$TAG" \
            --notes "$NOTES" 